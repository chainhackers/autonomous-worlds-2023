export enum Direction {
  Unknown,
  Up,
  Down,
  Left,
  Right
}

export enum Scenes {
  Main = "Main",
}

export enum Maps {
  Main = "Main",
}

export enum Animations {
  GolemIdle = "GolemIdle",
}
export enum Sprites {
  Golem,
  Player,
}

export enum Assets {
  MainAtlas = "MainAtlas",
  Tileset = "Tileset",
  Player = "Player",
}

export const TILE_HEIGHT = 32;
export const TILE_WIDTH = 32;
